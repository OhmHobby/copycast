root (Tk)
 └── outer (Frame)
      └── card (Frame)
           ├── header (Frame)
           │    ├── title (Label)
           │    └── settings button (Button)
           └── list (Canvas)
                └── rows (Frame)
                     ├── row 1
                     ├── row 2
                     └── ...


Machine A (sender)                      Machine B (receiver)
─────────────────                       ────────────────────
UI thread                               UI thread
  │ user clicks "broadcast"               ▲ drains incoming_q on tick
  ▼                                       │
outgoing_q (queue.Queue)                incoming_q (queue.Queue)
  │                                       ▲
  ▼                                       │
Broadcaster thread ───── network ─────► Receiver thread
  drains outgoing_q                       listens on socket
  sends over socket                       puts packets in incoming_q